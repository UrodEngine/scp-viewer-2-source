using UnityEngine.UI;
using UnityEngine;

public class text_canspawn : MonoBehaviour
{
    /*========================================================================================================================*/
    //public CameraFOV    camFovInstance;
    public GameObject   texterUI_lockerTouch;
    /*========================================================================================================================*/

    private void Update(){
        //if (camFovInstance.UI_lockerTouch <= 0)     texterUI_lockerTouch.SetActive(false);
        //else                                        texterUI_lockerTouch.SetActive(true);
    }
}
